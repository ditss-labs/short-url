<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code - <%= shortId %> | ASUMA Link</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #6366f1;
            --secondary: #8b5cf6;
            --accent: #10b981;
        }
        
        body {
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            color: #f1f5f9;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            min-height: 100vh;
        }
        
        .glass {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.3),
                0 0 0 1px rgba(255, 255, 255, 0.05),
                inset 0 1px 0 rgba(255, 255, 255, 0.1);
        }
        
        .gradient-text {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
        }
        
        .gradient-border {
            position: relative;
            border: double 2px transparent;
            background-image: linear-gradient(#0f172a, #0f172a), 
                              linear-gradient(135deg, var(--primary), var(--secondary));
            background-origin: border-box;
            background-clip: padding-box, border-box;
        }
        
        .hover-glow:hover {
            box-shadow: 0 0 30px rgba(99, 102, 241, 0.4);
            transform: translateY(-2px);
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            position: relative;
            overflow: hidden;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        
        .btn-primary::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s ease;
        }
        
        .btn-primary:hover::after {
            left: 100%;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(99, 102, 241, 0.4);
        }
        
        .qr-container {
            padding: 20px;
            background: linear-gradient(45deg, #ffffff 0%, #f8fafc 100%);
            border-radius: 20px;
            box-shadow: 
                0 20px 40px rgba(0, 0, 0, 0.3),
                inset 0 1px 0 rgba(255, 255, 255, 0.8);
            position: relative;
            overflow: hidden;
        }
        
        .qr-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--primary), var(--secondary));
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .float {
            animation: float 6s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-15px) rotate(5deg); }
        }
        
        .pulse {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(99, 102, 241, 0.4); }
            70% { box-shadow: 0 0 0 15px rgba(99, 102, 241, 0); }
            100% { box-shadow: 0 0 0 0 rgba(99, 102, 241, 0); }
        }
        
        .scan-line {
            animation: scan 3s linear infinite;
        }
        
        @keyframes scan {
            0% { top: 0; }
            100% { top: 100%; }
        }
        
        .glass-input {
            background: rgba(255, 255, 255, 0.07);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            color: white;
            transition: all 0.3s ease;
        }
        
        .glass-input:focus {
            background: rgba(255, 255, 255, 0.12);
            border-color: rgba(99, 102, 241, 0.8);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
        }
        
        .glass-input::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }
        
        .download-btn {
            position: relative;
            overflow: hidden;
        }
        
        .download-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }
        
        .download-btn:hover::before {
            width: 300px;
            height: 300px;
        }
        
        .floating-elements {
            position: absolute;
            pointer-events: none;
            z-index: 1;
        }
        
        .text-glow {
            text-shadow: 0 0 20px rgba(139, 92, 246, 0.5);
        }
        
        .qr-pattern {
            background: 
                radial-gradient(circle at 25% 25%, rgba(99, 102, 241, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(139, 92, 246, 0.1) 0%, transparent 50%);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-4 relative overflow-hidden">
    <!-- Floating Background Elements -->
    <div class="floating-elements top-10 left-10 w-32 h-32 bg-gradient-to-r from-purple-500/10 to-pink-500/10 rounded-full blur-3xl"></div>
    <div class="floating-elements bottom-10 right-10 w-40 h-40 bg-gradient-to-r from-blue-500/10 to-cyan-500/10 rounded-full blur-3xl" style="animation-delay: -2s;"></div>
    <div class="floating-elements top-1/3 right-1/4 w-8 h-8 bg-gradient-to-r from-emerald-400/30 to-green-400/30 rounded-full float" style="animation-delay: -1s;"></div>
    <div class="floating-elements bottom-1/4 left-1/4 w-6 h-6 bg-gradient-to-r from-blue-400/30 to-indigo-400/30 rounded-full float" style="animation-delay: -3s;"></div>

    <div class="max-w-4xl w-full z-10">
        <div class="glass-card rounded-3xl overflow-hidden fade-in">
            <!-- Header Section -->
            <div class="p-8 pb-6 bg-gradient-to-r from-gray-900/50 to-gray-800/50 border-b border-gray-800/50">
                <div class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6">
                    <div class="flex items-center gap-4">
                        <div class="gradient-bg p-4 rounded-2xl shadow-xl float">
                            <i class="fas fa-qrcode text-2xl text-white"></i>
                        </div>
                        <div>
                            <h1 class="text-3xl font-bold gradient-text">QR Code Generator</h1>
                            <p class="text-gray-400 flex items-center gap-2 mt-1">
                                <i class="fas fa-bolt text-amber-400"></i>
                                Dynamic QR code for <span class="font-mono text-white"><%= shortId %></span>
                            </p>
                        </div>
                    </div>
                    
                    <div class="flex flex-wrap gap-3">
                        <a href="<%= shortUrl %>" target="_blank"
                           class="px-5 py-3 glass rounded-xl hover:bg-white/10 transition flex items-center gap-2">
                            <i class="fas fa-external-link-alt"></i>
                            <span>Visit Link</span>
                        </a>
                        <a href="/dashboard"
                           class="px-5 py-3 btn-primary text-white rounded-xl flex items-center gap-2">
                            <i class="fas fa-tachometer-alt"></i>
                            <span>Dashboard</span>
                        </a>
                    </div>
                </div>
            </div>

            <div class="p-8">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                    <!-- QR Code Display Section -->
                    <div class="space-y-6 fade-in" style="animation-delay: 0.1s;">
                        <div class="relative">
                            <div class="qr-container mx-auto max-w-sm">
                                <div class="relative">
                                    <img src="<%= qrCode %>" alt="QR Code for <%= shortId %>" 
                                         class="w-full h-auto rounded-lg shadow-2xl">
                                    
                                    <!-- Scan Animation Overlay -->
                                    <div class="absolute inset-0 overflow-hidden rounded-lg">
                                        <div class="absolute left-0 right-0 h-0.5 bg-gradient-to-r from-transparent via-emerald-400 to-transparent scan-line"></div>
                                    </div>
                                    
                                    <!-- Center Logo Overlay -->
                                    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
                                        <div class="w-16 h-16 gradient-bg rounded-xl flex items-center justify-center">
                                            <i class="fas fa-link text-white text-xl"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            
                            <!-- QR Status Badges -->
                            <div class="flex flex-wrap justify-center gap-3 mt-6">
                                <div class="px-4 py-2 bg-gradient-to-r from-emerald-500/20 to-green-500/20 border border-emerald-500/30 rounded-xl flex items-center gap-2">
                                    <div class="w-2 h-2 bg-emerald-400 rounded-full pulse"></div>
                                    <span class="text-sm">Live QR Code</span>
                                </div>
                                <div class="px-4 py-2 bg-gradient-to-r from-blue-500/20 to-cyan-500/20 border border-blue-500/30 rounded-xl flex items-center gap-2">
                                    <i class="fas fa-sync-alt text-blue-400"></i>
                                    <span class="text-sm">Dynamic Updates</span>
                                </div>
                            </div>
                        </div>

                        <!-- Download Options -->
                        <div class="glass rounded-2xl p-6">
                            <h3 class="font-bold text-gray-300 mb-4 flex items-center gap-2">
                                <i class="fas fa-download text-emerald-400"></i>
                                Download QR Code
                            </h3>
                            <div class="grid grid-cols-2 gap-3">
                                <button onclick="downloadQR('png')"
                                        class="download-btn py-3 bg-gradient-to-r from-gray-800 to-gray-900 text-white rounded-xl hover:from-gray-700 hover:to-gray-800 transition flex items-center justify-center gap-2">
                                    <i class="fas fa-file-image text-blue-400"></i>
                                    <span>PNG Image</span>
                                </button>
                                <button onclick="downloadQR('svg')"
                                        class="download-btn py-3 bg-gradient-to-r from-gray-800 to-gray-900 text-white rounded-xl hover:from-gray-700 hover:to-gray-800 transition flex items-center justify-center gap-2">
                                    <i class="fas fa-file-code text-purple-400"></i>
                                    <span>SVG Vector</span>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Information & Actions Section -->
                    <div class="space-y-6 fade-in" style="animation-delay: 0.2s;">
                        <!-- URL Display -->
                        <div class="glass rounded-2xl p-6">
                            <h3 class="font-bold text-gray-300 mb-4 flex items-center gap-2">
                                <i class="fas fa-link text-purple-400"></i>
                                Short URL Details
                            </h3>
                            
                            <div class="space-y-4">
                                <div>
                                    <label class="block text-sm text-gray-400 mb-2">Short URL</label>
                                    <div class="flex group">
                                        <input type="text" value="<%= shortUrl %>" readonly
                                               class="flex-1 px-5 py-4 glass-input rounded-l-xl font-mono text-lg truncate">
                                        <button onclick="copyShortUrl('<%= shortUrl %>')"
                                                class="px-6 py-4 gradient-bg text-white rounded-r-xl hover-glow transition flex items-center gap-2">
                                            <i class="fas fa-copy" id="copyIcon"></i>
                                            <span id="copyText">Copy</span>
                                        </button>
                                    </div>
                                </div>
                                
                                <div class="grid grid-cols-2 gap-4">
                                    <div class="p-4 gradient-border rounded-xl text-center">
                                        <div class="text-2xl font-bold text-white mb-1"><%= clicks %></div>
                                        <div class="text-sm text-gray-400">Total Clicks</div>
                                    </div>
                                    <div class="p-4 gradient-border rounded-xl text-center">
                                        <div class="text-xl font-bold text-white mb-1 font-mono"><%= shortId %></div>
                                        <div class="text-sm text-gray-400">Short ID</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- QR Customization -->
                        <div class="glass rounded-2xl p-6">
                            <h3 class="font-bold text-gray-300 mb-4 flex items-center gap-2">
                                <i class="fas fa-palette text-amber-400"></i>
                                Customize QR Code
                            </h3>
                            
                            <div class="space-y-4">
                                <div>
                                    <label class="block text-sm text-gray-400 mb-2">Color Theme</label>
                                    <div class="flex gap-2">
                                        <button class="w-10 h-10 bg-gradient-to-r from-purple-500 to-indigo-500 rounded-lg border-2 border-white"></button>
                                        <button class="w-10 h-10 bg-gradient-to-r from-blue-500 to-cyan-500 rounded-lg border-2 border-gray-800"></button>
                                        <button class="w-10 h-10 bg-gradient-to-r from-emerald-500 to-green-500 rounded-lg border-2 border-gray-800"></button>
                                        <button class="w-10 h-10 bg-gradient-to-r from-amber-500 to-orange-500 rounded-lg border-2 border-gray-800"></button>
                                    </div>
                                </div>
                                
                                <div>
                                    <label class="block text-sm text-gray-400 mb-2">QR Size</label>
                                    <div class="flex items-center gap-3">
                                        <span class="text-sm">Small</span>
                                        <div class="flex-1 h-2 bg-gray-800 rounded-full">
                                            <div class="w-2/3 h-full bg-gradient-to-r from-purple-500 to-indigo-500 rounded-full"></div>
                                        </div>
                                        <span class="text-sm">Large</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Quick Actions -->
                        <div class="glass rounded-2xl p-6">
                            <h3 class="font-bold text-gray-300 mb-4 flex items-center gap-2">
                                <i class="fas fa-bolt text-blue-400"></i>
                                Quick Actions
                            </h3>
                            
                            <div class="grid grid-cols-2 gap-3">
                                <a href="/<%= shortId %>/stats"
                                   class="p-4 bg-gradient-to-r from-gray-800 to-gray-900 rounded-xl hover:from-gray-700 hover:to-gray-800 transition flex flex-col items-center justify-center gap-2">
                                    <i class="fas fa-chart-line text-2xl text-emerald-400"></i>
                                    <span class="text-sm">View Analytics</span>
                                </a>
                                <a href="/"
                                   class="p-4 bg-gradient-to-r from-gray-800 to-gray-900 rounded-xl hover:from-gray-700 hover:to-gray-800 transition flex flex-col items-center justify-center gap-2">
                                    <i class="fas fa-plus-circle text-2xl text-purple-400"></i>
                                    <span class="text-sm">Create New</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Usage Instructions -->
                <div class="glass rounded-2xl p-6 mt-8 fade-in" style="animation-delay: 0.3s;">
                    <h3 class="font-bold text-gray-300 mb-4 flex items-center gap-2">
                        <i class="fas fa-lightbulb text-yellow-400"></i>
                        How to Use This QR Code
                    </h3>
                    
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="flex items-start gap-3 p-4 rounded-xl bg-gradient-to-r from-gray-800/50 to-gray-900/50">
                            <div class="p-2 bg-gradient-to-br from-blue-500/20 to-cyan-500/20 rounded-lg">
                                <i class="fas fa-mobile-alt text-blue-40
