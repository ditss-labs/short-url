<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Code - <%= shortId %> | ASUMA Link</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-purple-50 to-blue-50 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full">
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden">
            <div class="p-8">
                <div class="text-center mb-8">
                    <div class="inline-flex items-center justify-center p-3 bg-gradient-to-r from-purple-600 to-blue-600 rounded-2xl shadow-lg mb-6">
                        <i class="fas fa-qrcode text-3xl text-white"></i>
                    </div>
                    <h1 class="text-3xl font-bold text-gray-800 mb-2">QR Code Generated</h1>
                    <p class="text-gray-600">Scan this QR code to visit the link</p>
                </div>

                <div class="flex justify-center mb-8">
                    <div class="p-6 bg-white border-4 border-purple-100 rounded-2xl shadow-lg">
                        <img src="<%= qrCode %>" alt="QR Code for <%= shortId %>" class="w-64 h-64">
                    </div>
                </div>

                <div class="space-y-6">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-2">
                            <i class="fas fa-link mr-2"></i> Short URL
                        </label>
                        <div class="flex items-center">
                            <input type="text" value="<%= shortUrl %>" readonly
                                   class="flex-1 px-4 py-3 border-2 border-gray-300 rounded-l-xl bg-gray-50 font-mono">
                            <button onclick="copyToClipboard('<%= shortUrl %>')"
                                    class="px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 text-white rounded-r-xl hover:from-purple-700 hover:to-blue-700 transition">
                                <i class="fas fa-copy"></i>
                            </button>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div class="bg-gray-50 p-4 rounded-xl">
                            <label class="block text-sm font-medium text-gray-600 mb-1">Clicks</label>
                            <p class="text-2xl font-bold text-purple-600"><%= clicks %></p>
                        </div>
                        <div class="bg-gray-50 p-4 rounded-xl">
                            <label class="block text-sm font-medium text-gray-600 mb-1">ID</label>
                            <p class="text-xl font-semibold text-gray-800"><%= shortId %></p>
                        </div>
                    </div>

                    <div class="flex flex-col sm:flex-row gap-4">
                        <a href="<%= shortUrl %>" target="_blank"
                           class="flex-1 py-3 px-6 bg-gradient-to-r from-green-500 to-emerald-600 text-white rounded-xl hover:from-green-600 hover:to-emerald-700 transition text-center flex items-center justify-center shadow-lg">
                            <i class="fas fa-external-link-alt mr-3"></i>Visit Link
                        </a>
                        <a href="/dashboard"
                           class="flex-1 py-3 px-6 bg-gradient-to-r from-blue-500 to-cyan-600 text-white rounded-xl hover:from-blue-600 hover:to-cyan-700 transition text-center flex items-center justify-center shadow-lg">
                            <i class="fas fa-tachometer-alt mr-3"></i>Dashboard
                        </a>
                    </div>
                </div>

                <div class="mt-8 pt-6 border-t border-gray-200 text-center">
                    <a href="/" class="text-purple-600 hover:text-purple-800 font-medium">
                        <i class="fas fa-plus mr-2"></i>Create Another Short URL
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script>
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                alert('Copied to clipboard!');
            });
        }
    </script>
</body>
</html>
