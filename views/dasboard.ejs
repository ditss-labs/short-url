<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - ASUMA Link</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-8">
            <div>
                <h1 class="text-4xl font-bold text-gray-800">
                    <i class="fas fa-tachometer-alt mr-3"></i>Dashboard
                </h1>
                <p class="text-gray-600 mt-2">Monitor and manage your shortened URLs</p>
            </div>
            <a href="/" class="mt-4 md:mt-0 px-6 py-3 bg-gradient-to-r from-purple-600 to-blue-600 text-white rounded-xl hover:from-purple-700 hover:to-blue-700 transition shadow-lg flex items-center">
                <i class="fas fa-plus mr-2"></i>Create New
            </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
            <div class="bg-white p-6 rounded-xl shadow-lg">
                <div class="flex items-center">
                    <div class="p-3 bg-purple-100 rounded-lg mr-4">
                        <i class="fas fa-link text-purple-600 text-2xl"></i>
                    </div>
                    <div>
                        <p class="text-gray-600">Total URLs</p>
                        <p class="text-3xl font-bold text-gray-800"><%= totalUrls %></p>
                    </div>
                </div>
            </div>
            
            <div class="bg-white p-6 rounded-xl shadow-lg">
                <div class="flex items-center">
                    <div class="p-3 bg-blue-100 rounded-lg mr-4">
                        <i class="fas fa-mouse-pointer text-blue-600 text-2xl"></i>
                    </div>
                    <div>
                        <p class="text-gray-600">Total Clicks</p>
                        <p class="text-3xl font-bold text-gray-800"><%= totalClicks %></p>
                    </div>
                </div>
            </div>
            
            <div class="bg-white p-6 rounded-xl shadow-lg">
                <div class="flex items-center">
                    <div class="p-3 bg-green-100 rounded-lg mr-4">
                        <i class="fas fa-chart-line text-green-600 text-2xl"></i>
                    </div>
                    <div>
                        <p class="text-gray-600">Today's Clicks</p>
                        <p class="text-3xl font-bold text-gray-800"><%= todayClicks %></p>
                    </div>
                </div>
            </div>
            
            <div class="bg-white p-6 rounded-xl shadow-lg">
                <div class="flex items-center">
                    <div class="p-3 bg-yellow-100 rounded-lg mr-4">
                        <i class="fas fa-clock text-yellow-600 text-2xl"></i>
                    </div>
                    <div>
                        <p class="text-gray-600">Active URLs</p>
                        <p class="text-3xl font-bold text-gray-800">
                            <%= urls.filter(url => !url.expiresAt || new Date(url.expiresAt) > new Date()).length %>
                        </p>
                    </div>
                </div>
            </div>
        </div>

        <div class="bg-white rounded-xl shadow-lg overflow-hidden">
            <div class="px-6 py-4 border-b border-gray-200">
                <h2 class="text-xl font-semibold text-gray-800">Recent URLs</h2>
            </div>
            
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Short URL</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Original URL</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Clicks</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Created</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <% urls.forEach(url => { %>
                        <tr class="hover:bg-gray-50 transition">
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <i class="fas fa-link text-gray-400 mr-2"></i>
                                    <a href="/<%= url.shortId %>" target="_blank" 
                                       class="text-purple-600 hover:text-purple-800 font-medium">
                                        <%= APP_DOMAIN %>/<%= url.shortId %>
                                    </a>
                                    <button onclick="copyToClipboard('<%= APP_DOMAIN %>/<%= url.shortId %>')"
                                            class="ml-2 text-gray-400 hover:text-gray-600">
                                        <i class="fas fa-copy"></i>
                                    </button>
                                </div>
                                <% if (url.requiresPassword) { %>
                                <span class="text-xs text-green-600 mt-1 flex items-center">
                                    <i class="fas fa-lock mr-1"></i> Password
                                </span>
                                <% } %>
                            </td>
                            <td class="px-6 py-4">
                                <div class="max-w-xs truncate text-gray-600" title="<%= url.originalUrl %>">
                                    <%= url.originalUrl %>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <span class="px-3 py-1 rounded-full text-sm font-semibold 
                                    <%= url.clicks > 100 ? 'bg-green-100 text-green-800' : 
                                        url.clicks > 10 ? 'bg-blue-100 text-blue-800' : 
                                        'bg-gray-100 text-gray-800' %>">
                                    <%= url.clicks %> clicks
                                </span>
                            </td>
                            <td class="px-6 py-4 text-gray-600">
                                <%= new Date(url.createdAt).toLocaleDateString() %>
                                <div class="text-sm text-gray-500">
                                    <% if (url.expiresAt) { %>
                                    Expires: <%= new Date(url.expiresAt).toLocaleDateString() %>
                                    <% } else { %>
                                    No expiration
                                    <% } %>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex space-x-2">
                                    <a href="/<%= url.shortId %>" target="_blank"
                                       class="px-3 py-1 bg-green-100 text-green-700 rounded-lg text-sm hover:bg-green-200 transition flex items-center">
                                        <i class="fas fa-external-link-alt mr-1"></i>Visit
                                    </a>
                                    <a href="/<%= url.shortId %>/stats"
                                       class="px-3 py-1 bg-blue-100 text-blue-700 rounded-lg text-sm hover:bg-blue-200 transition flex items-center">
                                        <i class="fas fa-chart-bar mr-1"></i>Stats
                                    </a>
                                    <a href="/<%= url.shortId %>/qr"
                                       class="px-3 py-1 bg-purple-100 text-purple-700 rounded-lg text-sm hover:bg-purple-200 transition flex items-center">
                                        <i class="fas fa-qrcode mr-1"></i>QR
                                    </a>
                                </div>
                            </td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
            
            <% if (urls.length === 0) { %>
            <div class="text-center py-12">
                <i class="fas fa-inbox text-4xl text-gray-300 mb-4"></i>
                <p class="text-gray-500">No URLs created yet</p>
                <a href="/" class="mt-4 inline-block text-purple-600 hover:text-purple-800 font-medium">
                    <i class="fas fa-plus mr-2"></i>Create your first short URL
                </a>
            </div>
            <% } %>
        </div>
    </div>

    <script>
        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                alert('URL copied to clipboard!');
            });
        }
    </script>
</body>
</html>
