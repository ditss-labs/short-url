<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - ASUMA Link</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        }
        
        .glass-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.9);
            box-shadow: 
                0 4px 6px -1px rgba(0, 0, 0, 0.05),
                0 2px 4px -1px rgba(0, 0, 0, 0.03),
                inset 0 1px 0 rgba(255, 255, 255, 1);
        }
        
        .stat-card {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border-bottom: 4px solid transparent;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px -4px rgba(0, 0, 0, 0.08);
        }
        
        .stat-card:nth-child(1):hover { border-bottom-color: #8b5cf6; }
        .stat-card:nth-child(2):hover { border-bottom-color: #3b82f6; }
        .stat-card:nth-child(3):hover { border-bottom-color: #10b981; }
        .stat-card:nth-child(4):hover { border-bottom-color: #f59e0b; }
        
        .gradient-btn {
            background: linear-gradient(135deg, #8b5cf6 0%, #6366f1 100%);
            position: relative;
            overflow: hidden;
        }
        
        .gradient-btn::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }
        
        .gradient-btn:hover::after {
            left: 100%;
        }
        
        .url-row {
            transition: all 0.2s ease;
            border-left: 4px solid transparent;
        }
        
        .url-row:hover {
            border-left-color: #8b5cf6;
            background: linear-gradient(90deg, rgba(139, 92, 246, 0.05) 0%, transparent 100%);
        }
        
        .click-badge {
            transition: all 0.3s ease;
        }
        
        .click-badge:hover {
            transform: scale(1.05);
        }
        
        .truncate-url {
            max-width: 300px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            display: inline-block;
            vertical-align: bottom;
        }
        
        .action-btn {
            transition: all 0.2s ease;
            opacity: 0.8;
        }
        
        .action-btn:hover {
            opacity: 1;
            transform: translateY(-1px);
        }
        
        .pulse {
            animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
        }
        
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .highlight {
            background: linear-gradient(120deg, rgba(139, 92, 246, 0.15) 0%, transparent 100%);
            border-radius: 8px;
            padding: 2px 6px;
        }
    </style>
</head>
<body class="min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-7xl">
        <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center mb-10 gap-6 fade-in">
            <div class="flex-1">
                <div class="flex items-center gap-4 mb-4">
                    <div class="p-3 bg-gradient-to-br from-purple-500 to-indigo-600 rounded-2xl shadow-lg">
                        <i class="fas fa-tachometer-alt-fast text-2xl text-white"></i>
                    </div>
                    <div>
                        <h1 class="text-4xl font-bold bg-gradient-to-r from-gray-800 to-gray-900 bg-clip-text text-transparent">
                            Dashboard Analytics
                        </h1>
                        <p class="text-gray-600 mt-2 flex items-center gap-2">
                            <i class="fas fa-chart-line text-purple-500"></i>
                            Real-time monitoring for your shortened URLs
                        </p>
                    </div>
                </div>
                
                <div class="flex flex-wrap gap-3 mt-6">
                    <div class="px-4 py-2 bg-gradient-to-r from-purple-50 to-indigo-50 rounded-xl border border-purple-100">
                        <span class="text-sm text-gray-600">Total URLs</span>
                        <span class="ml-2 font-bold text-purple-700"><%= totalUrls %></span>
                    </div>
                    <div class="px-4 py-2 bg-gradient-to-r from-blue-50 to-cyan-50 rounded-xl border border-blue-100">
                        <span class="text-sm text-gray-600">Total Clicks</span>
                        <span class="ml-2 font-bold text-blue-700"><%= totalClicks %></span>
                    </div>
                    <div class="px-4 py-2 bg-gradient-to-r from-emerald-50 to-green-50 rounded-xl border border-emerald-100">
                        <span class="text-sm text-gray-600">Active Now</span>
                        <span class="ml-2 font-bold text-emerald-700">
                            <%= urls.filter(url => !url.expiresAt || new Date(url.expiresAt) > new Date()).length %>
                        </span>
                    </div>
                </div>
            </div>
            
            <a href="/" class="gradient-btn px-8 py-4 text-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 font-semibold flex items-center gap-3 group">
                <i class="fas fa-plus-circle text-lg"></i>
                <span>Create New Link</span>
                <i class="fas fa-arrow-right group-hover:translate-x-2 transition-transform duration-300"></i>
            </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10 fade-in" style="animation-delay: 0.1s;">
            <div class="glass-card stat-card p-6 rounded-2xl">
                <div class="flex items-start justify-between mb-4">
                    <div class="p-3 bg-gradient-to-br from-purple-100 to-purple-50 rounded-xl">
                        <i class="fas fa-link text-2xl text-purple-600"></i>
                    </div>
                    <div class="text-right">
                        <div class="text-2xl font-bold text-gray-900"><%= totalUrls %></div>
                        <div class="text-sm text-purple-600 font-medium">+12%</div>
                    </div>
                </div>
                <h3 class="text-gray-700 font-semibold mb-1">Total URLs</h3>
                <p class="text-gray-500 text-sm">Shortened links created</p>
            </div>
            
            <div class="glass-card stat-card p-6 rounded-2xl">
                <div class="flex items-start justify-between mb-4">
                    <div class="p-3 bg-gradient-to-br from-blue-100 to-blue-50 rounded-xl">
                        <i class="fas fa-mouse-pointer text-2xl text-blue-600"></i>
                    </div>
                    <div class="text-right">
                        <div class="text-2xl font-bold text-gray-900"><%= totalClicks %></div>
                        <div class="text-sm text-blue-600 font-medium">+24%</div>
                    </div>
                </div>
                <h3 class="text-gray-700 font-semibold mb-1">Total Clicks</h3>
                <p class="text-gray-500 text-sm">All-time engagements</p>
            </div>
            
            <div class="glass-card stat-card p-6 rounded-2xl">
                <div class="flex items-start justify-between mb-4">
                    <div class="p-3 bg-gradient-to-br from-emerald-100 to-emerald-50 rounded-xl">
                        <i class="fas fa-chart-line text-2xl text-emerald-600"></i>
                    </div>
                    <div class="text-right">
                        <div class="text-2xl font-bold text-gray-900"><%= todayClicks %></div>
                        <div class="text-sm text-emerald-600 font-medium pulse">LIVE</div>
                    </div>
                </div>
                <h3 class="text-gray-700 font-semibold mb-1">Today's Clicks</h3>
                <p class="text-gray-500 text-sm">Real-time activity</p>
            </div>
            
            <div class="glass-card stat-card p-6 rounded-2xl">
                <div class="flex items-start justify-between mb-4">
                    <div class="p-3 bg-gradient-to-br from-amber-100 to-amber-50 rounded-xl">
                        <i class="fas fa-clock text-2xl text-amber-600"></i>
                    </div>
                    <div class="text-right">
                        <div class="text-2xl font-bold text-gray-900">
                            <%= urls.filter(url => !url.expiresAt || new Date(url.expiresAt) > new Date()).length %>
                        </div>
                        <div class="text-sm text-amber-600 font-medium">Active</div>
                    </div>
                </div>
                <h3 class="text-gray-700 font-semibold mb-1">Active URLs</h3>
                <p class="text-gray-500 text-sm">Currently accessible</p>
            </div>
        </div>

        <div class="glass-card rounded-2xl overflow-hidden shadow-xl mb-10 fade-in" style="animation-delay: 0.2s;">
            <div class="px-8 py-6 border-b border-gray-100 bg-gradient-to-r from-gray-50 to-white">
                <div class="flex items-center justify-between">
                    <div>
                        <h2 class="text-2xl font-bold text-gray-900">Your Links</h2>
                        <p class="text-gray-600 mt-1">Manage and track all your shortened URLs</p>
                    </div>
                    <div class="text-sm text-gray-500">
                        Showing <span class="font-bold text-gray-800"><%= urls.length %></span> links
                    </div>
                </div>
            </div>
            
            <% if (urls.length === 0) { %>
            <div class="text-center py-16 px-4">
                <div class="inline-flex p-6 bg-gradient-to-br from-gray-100 to-gray-50 rounded-3xl mb-6">
                    <i class="fas fa-inbox text-5xl text-gray-300"></i>
                </div>
                <h3 class="text-xl font-semibold text-gray-700 mb-3">No URLs Created Yet</h3>
                <p class="text-gray-500 mb-8 max-w-md mx-auto">Start by creating your first shortened link to track analytics and manage your URLs</p>
                <a href="/" class="gradient-btn px-8 py-3 text-white rounded-xl font-semibold inline-flex items-center gap-2">
                    <i class="fas fa-plus"></i>
                    Create First URL
                </a>
            </div>
            <% } else { %>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead>
                        <tr class="bg-gradient-to-r from-gray-50 to-gray-100">
                            <th class="px-8 py-4 text-left font-semibold text-gray-700">Short URL</th>
                            <th class="px-8 py-4 text-left font-semibold text-gray-700">Destination</th>
                            <th class="px-8 py-4 text-left font-semibold text-gray-700">Engagement</th>
                            <th class="px-8 py-4 text-left font-semibold text-gray-700">Status</th>
                            <th class="px-8 py-4 text-left font-semibold text-gray-700">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% urls.forEach((url, index) => { %>
                        <tr class="url-row fade-in" style="animation-delay: <%= 0.3 + (index * 0.05) %>s;">
                            <td class="px-8 py-5">
                                <div class="flex flex-col gap-2">
                                    <div class="flex items-center group">
                                        <div class="p-2 bg-gradient-to-br from-purple-50 to-indigo-50 rounded-lg mr-3">
                                            <i class="fas fa-link text-purple-600"></i>
                                        </div>
                                        <div>
                                            <a href="/<%= url.shortId %>" target="_blank" 
                                               class="font-bold text-gray-900 hover:text-purple-700 transition-colors group-hover:underline">
                                                <%= APP_DOMAIN %>/<span class="highlight"><%= url.shortId %></span>
                                            </a>
                                            <button onclick="copyShortUrl('<%= APP_DOMAIN %>/<%= url.shortId %>', this)"
                                                    class="ml-3 action-btn p-1.5 text-gray-400 hover:text-purple-600 rounded-lg"
                                                    title="Copy URL">
                                                <i class="fas fa-copy text-sm"></i>
                                            </button>
                                        </div>
                                    </div>
                                    <% if (url.requiresPassword) { %>
                                    <div class="flex items-center gap-1.5 text-xs">
                                        <span class="px-2.5 py-1 bg-gradient-to-r from-emerald-50 to-green-50 text-emerald-700 rounded-full font-medium border border-emerald-100">
                                            <i class="fas fa-lock mr-1.5"></i> Password Protected
                                        </span>
                                    </div>
                                    <% } %>
                                </div>
                            </td>
                            <td class="px-8 py-5">
                                <div class="flex items-start max-w-xs">
                                    <i class="fas fa-arrow-right text-gray-300 mt-1 mr-3"></i>
                                    <div>
                                        <div class="truncate-url text-gray-600" title="<%= url.originalUrl %>">
                                            <%= url.originalUrl %>
                                        </div>
                                        <div class="text-xs text-gray-400 mt-1 flex items-center gap-2">
                                            <i class="far fa-calendar"></i>
                                            <%= new Date(url.createdAt).toLocaleDateString('en-US', { 
                                                month: 'short', 
                                                day: 'numeric',
                                                year: 'numeric'
                                            }) %>
                                        </div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-8 py-5">
                                <div class="flex items-center">
                                    <div class="text-center">
                                        <span class="click-badge px-4 py-2 rounded-xl text-lg font-bold 
                                            <%= url.clicks > 100 ? 'bg-gradient-to-r from-emerald-100 to-green-100 text-emerald-800' : 
                                               url.clicks > 10 ? 'bg-gradient-to-r from-blue-100 to-cyan-100 text-blue-800' : 
                                               'bg-gradient-to-r from-gray-100 to-gray-50 text-gray-700' %>">
                                            <%= url.clicks %>
                                        </span>
                                        <div class="text-xs text-gray-500 mt-1.5">clicks</div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-8 py-5">
                                <div class="flex flex-col gap-2">
                                    <div class="flex items-center">
                                        <% if (!url.expiresAt || new Date(url.expiresAt) > new Date()) { %>
                                        <div class="flex items-center gap-2">
                                            <div class="w-2 h-2 bg-emerald-500 rounded-full"></div>
                                            <span class="font-medium text-emerald-700">Active</span>
                                        </div>
                                        <% } else { %>
                                        <div class="flex items-center gap-2">
                                            <div class="w-2 h-2 bg-gray-400 rounded-full"></div>
                                            <span class="font-medium text-gray-600">Expired</span>
                                        </div>
                                        <% } %>
                                    </div>
                                    <% if (url.expiresAt) { %>
                                    <div class="text-xs text-gray-500">
                                        <i class="far fa-clock mr-1"></i>
                                        <%= new Date(url.expiresAt).toLocaleDateString() %>
                                    </div>
                                    <% } else { %>
                                    <div class="text-xs text-gray-500">
                                        <i class="fas fa-infinity mr-1"></i>
                                        No expiration
                                    </div>
                                    <% } %>
                                </div>
                            </td>
                            <td class="px-8 py-5">
                                <div class="flex items-center gap-2">
                                    <a href="/<%= url.shortId %>" target="_blank"
                                       class="action-btn p-3 bg-gradient-to-r from-blue-50 to-cyan-50 text-blue-700 rounded-xl hover:from-blue-100 hover:to-cyan-100"
                                       title="Visit">
                                        <i class="fas fa-external-link-alt"></i>
                                    </a>
                                    <a href="/<%= url.shortId %>/stats"
                                       class="action-btn p-3 bg-gradient-to-r from-purple-50 to-indigo-50 text-purple-700 rounded-xl hover:from-purple-100 hover:to-indigo-100"
                                       title="Analytics">
                                        <i class="fas fa-chart-bar"></i>
                                    </a>
                                    <a href="/<%= url.shortId %>/qr"
                                       class="action-btn p-3 bg-gradient-to-r from-emerald-50 to-green-50 text-emerald-700 rounded-xl hover:from-emerald-100 hover:to-green-100"
                                       title="QR Code">
                                        <i class="fas fa-qrcode"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
            <% } %>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 fade-in" style="animation-delay: 0.4s;">
            <div class="glass-card p-6 rounded-2xl">
                <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                    <i class="fas fa-bolt text-amber-500"></i>
                    Quick Actions
                </h3>
                <div class="space-y-3">
                    <button onclick="exportAllUrls()" class="w-full p-4 rounded-xl border border-gray-200 hover:border-purple-300 hover:bg-purple-50 transition-all duration-200 flex items-center justify-between group">
                        <div class="flex items-center gap-3">
                            <div class="p-2 bg-blue-100 rounded-lg">
                                <i class="fas fa-file-export text-blue-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">Export All URLs</span>
                        </div>
                        <i class="fas fa-arrow-right text-gray-400 group-hover:text-purple-600 group-hover:translate-x-1 transition-all"></i>
                    </button>
                    <button onclick="showQRCodes()" class="w-full p-4 rounded-xl border border-gray-200 hover:border-emerald-300 hover:bg-emerald-50 transition-all duration-200 flex items-center justify-between group">
                        <div class="flex items-center gap-3">
                            <div class="p-2 bg-emerald-100 rounded-lg">
                                <i class="fas fa-qrcode text-emerald-600"></i>
                            </div>
                            <span class="font-medium text-gray-700">Generate QR Codes</span>
                        </div>
                        <i class="fas fa-arrow-right text-gray-400 group-hover:text-emerald-600 group-hover:translate-x-1 transition-all"></i>
                    </button>
                </div>
            </div>
            
            <div class="glass-card p-6 rounded-2xl">
                <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                    <i class="fas fa-chart-pie text-purple-500"></i>
                    Performance
                </h3>
                <div class="space-y-4">
                    <div>
                        <div class="flex justify-between text-sm text-gray-600 mb-1">
                            <span>Click Rate</span>
                            <span><%= Math.round((totalClicks / Math.max(totalUrls, 1))) %> per URL</span>
                        </div>
                        <div class="w-full bg-gray-200 rounded-full h-2">
                            <div class="bg-gradient-to-r from-purple-500 to-indigo-500 h-2 rounded-full" 
                                 style="width: <%= Math.min(100, (totalClicks / Math.max(totalUrls, 1)) * 10) %>%"></div>
                        </div>
                    </div>
                    <div>
                        <div class="flex justify-between text-sm text-gray-600 mb-1">
                            <span>Active Rate</span>
                            <span><%= Math.round((urls.filter(url => !url.expiresAt || new Date(url.expiresAt) > new Date()).length / Math.max(urls.length, 1)) * 100) %>%</span>
                        </div>
                        <div class="w-full bg-gray-200 rounded-full h-2">
                            <div class="bg-gradient-to-r from-emerald-500 to-green-500 h-2 rounded-full" 
                                 style="width: <%= (urls.filter(url => !url.expiresAt || new Date(url.expiresAt) > new Date()).length / Math.max(urls.length, 1)) * 100 %>%"></div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="glass-card p-6 rounded-2xl">
                <h3 class="font-bold text-gray-900 mb-4 flex items-center gap-2">
                    <i class="fas fa-crown text-amber-500"></i>
                    Top Links
                </h3>
                <div class="space-y-3">
                    <% const topLinks = urls.sort((a, b) => b.clicks - a.clicks).slice(0, 3); %>
                    <% topLinks.forEach(link => { %>
                    <div class="flex items-center justify-between p-3 rounded-lg bg-gradient-to-r from-gray-50 to-white hover:from-gray-100 transition-all">
                        <div class="flex items-center gap-3">
                            <div class="w-8 h-8 flex items-center justify-center bg-gradient-to-br from-purple-100 to-indigo-100 rounded-lg">
                                <span class="font-bold text-purple-700 text-sm"><%= link.clicks %></span>
                            </div>
                            <div>
                                <div class="font-medium text-gray-900 text-sm truncate max-w-[120px]">
                                    <%= APP_DOMAIN %>/<%= link.shortId %>
                                </div>
                            </div>
                        </div>
                        <a href="/<%= link.shortId %>/stats" class="text-gray-400 hover:text-purple-600">
                            <i class="fas fa-chevron-right"></i>
                        </a>
                    </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>

    <script>
        async function copyShortUrl(url, button) {
            try {
                await navigator.clipboard.writeText(url);
                
                const icon = button.querySelector('i');
                const originalClass = icon.className;
                
                button.classList.add('!text-emerald-600');
                icon.className = 'fas fa-check';
                
                setTimeout(() => {
                    button.classList.remove('!text-emerald-600');
                    icon.className = originalClass;
                }, 2000);
            } catch (err) {
                alert('Failed to copy URL');
            }
        }
        
        function exportAllUrls() {
            const urls = [];
            document.querySelectorAll('.url-row').forEach(row => {
                const shortUrl = row.querySelector('a[href^="/"]').textContent.trim();
                urls.push(shortUrl);
            });
            
            const blob = new Blob([urls.join('\n')], { type: 'text/plain' });
            const url = URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'asuma-links-export.txt';
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);
            URL.revokeObjectURL(url);
            
            showToast('All URLs exported successfully!');
        }
        
        function showQRCodes() {
            showToast('QR Code generator coming soon!');
        }
        
        function showToast(message) {
            const toast = document.createElement('div');
            toast.className = 'fixed bottom-6 right-6 bg-gradient-to-r from-gray-900 to-gray-800 text-white px-6 py-3 rounded-xl shadow-2xl font-medium flex items-center gap-3 z-50 animate-fadeIn';
            toast.innerHTML = `
                <i class="fas fa-check-circle text-emerald-400"></i>
                <span>${message}</span>
            `;
            document.body.appendChild(toast);
            
            setTimeout(() => {
                toast.style.animation = 'fadeOut 0.3s ease-out';
                setTimeout(() => toast.remove(), 300);
            }, 3000);
            
            const style = document.createElement('style');
            style.textContent = `
                @keyframes fadeIn {
                    from { opacity: 0; transform: translateY(20px); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @keyframes fadeOut {
                    from { opacity: 1; transform: translateY(0); }
                    to { opacity: 0; transform: translateY(20px); }
                }
            `;
            document.head.appendChild(style);
        }
        
        document.addEventListener('DOMContentLoaded', () => {
            const statCards = document.querySelectorAll('.stat-card');
            statCards.forEach(card => {
                card.addEventListener('mouseenter', () => {
                    card.style.transform = 'translateY(-5px)';
                });
                card.addEventListener('mouseleave', () => {
                    card.style.transform = 'translateY(0)';
                });
            });
            
            const todayClicks = <%= todayClicks %>;
            if (todayClicks > 0) {
                const todayCard = document.querySelector('.pulse');
                setInterval(() => {
                    todayCard.classList.toggle('opacity-70');
                }, 2000);
            }
        });
    </script>
</body>
</html>
